/**
 * Configuración del entorno para producción.
 * 
 * Este archivo contiene las variables de entorno utilizadas en el entorno de producción.
 */
export const environment = {
  /** Indica si la aplicación se ejecuta en modo producción. */
  production: true,

  /** URL base de la API utilizada en producción. */
  apiUrl: 'http://speedorz.bucaramanga.upb.edu.co:8081'
};
